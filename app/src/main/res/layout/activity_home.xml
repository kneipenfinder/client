<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:fillViewport="true"
    >
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        style="@style/backgroundStyle"
        xmlns:tools="http://schemas.android.com/tools"
        tools:context=".home"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            style="@style/buttonStyleHome"
            android:text="@string/homefinden"
            android:id="@+id/find"
            android:onClick="button_findLocation"
            android:layout_weight=".50"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_margin="@dimen/activity_vertical_margin" />

        <Button
            style="@style/buttonStyleHome"
            android:text="@string/homesuchen"
            android:id="@+id/search"
            android:layout_weight=".50"
            android:layout_width="fill_parent"
            android:onClick="button_searchLocation"
            android:layout_height="fill_parent"
            android:layout_margin="@dimen/activity_vertical_margin" />

        <Button
            style="@style/buttonStyleHome"
            android:text="@string/homehinzufügen"
            android:id="@+id/add"
            android:layout_weight=".50"
            android:layout_width="fill_parent"
            android:onClick="button_addLocation"
            android:visibility="visible"
            android:layout_height="fill_parent"
            android:layout_margin="@dimen/activity_vertical_margin" />

    </LinearLayout>
</ScrollView>